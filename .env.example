# Optional tuning parameters — copy to .env and adjust as needed

# Target frames per second
FPS_TARGET=60

# Number of segments drawn per frame (higher = faster animation)
SEGMENTS_PER_FRAME=8

# Enable debug logging
DEBUG=

# Monitor index to use for the borderless fullscreen window (0 = primary)
DISPLAY_INDEX=0
